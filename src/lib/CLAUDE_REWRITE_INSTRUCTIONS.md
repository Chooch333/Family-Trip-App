// RENAME THIS FILE TO claude.ts (replacing the existing one)
// Then update the getPromptChips call site in trip/[tripId]/page.tsx:
//   getPromptChips(trip, days[activeDay] || null, currentDayStops)
//
// See commit message for full changelog.

export { } // placeholder — full file is in the downloaded artifact
